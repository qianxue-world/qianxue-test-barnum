apiVersion: v1
kind: Service
metadata:
  name: dktatlas-service
  namespace: dktatlas-ns
  labels:
    app: dktatlas
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: dktatlas
