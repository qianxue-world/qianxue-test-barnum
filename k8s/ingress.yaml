apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: qianxue-test-fatebook-ingress
  namespace: qianxue-test-fatebook-ns
  labels:
    app: qianxue-test-fatebook
  annotations:
    kubernetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/gzip-types: "text/plain,text/css,application/json,application/javascript,text/xml,application/xml,application/xml+rss,text/javascript"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - test-fatebook.qianxue.online
    secretName: qianxue-test-fatebook-tls-secret
  rules:
  - host: test-fatebook.qianxue.online
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: qianxue-test-fatebook-service
            port:
              number: 80
